<Window x:Class="MedCompanion.Dialogs.WhisperVocabDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="📝 Vocabulaire personnalisé Whisper"
        Width="580" Height="650"
        WindowStartupLocation="CenterOwner"
        Background="#FAFAFA"
        ResizeMode="CanResizeWithGrip">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Vocabulaire personnalisé Whisper"
                       FontSize="18" FontWeight="SemiBold" Foreground="#5E35B1"/>
            <TextBlock Text="Ces termes sont injectés dans le prompt Whisper pour améliorer la transcription médicale."
                       FontSize="12" Foreground="#7F8C8D" TextWrapping="Wrap" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Info -->
        <Border Grid.Row="1" Background="#EDE7F6" CornerRadius="6"
                Padding="10,8" Margin="0,0,0,10" BorderBrush="#B39DDB" BorderThickness="1">
            <StackPanel>
                <TextBlock FontSize="11" Foreground="#5E35B1" TextWrapping="Wrap">
                    <Run FontWeight="SemiBold">Format :</Run>
                    <Run>Un terme par ligne. Sections entre [crochets]. Commentaires avec #.</Run>
                </TextBlock>
                <TextBlock x:Name="StatsText" FontSize="10" Foreground="#9575CD" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Éditeur -->
        <TextBox Grid.Row="2" x:Name="VocabEditor"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas"
                 FontSize="12"
                 Padding="10"
                 BorderBrush="#B39DDB"
                 BorderThickness="1"
                 Background="White">
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TextBox">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="6">
                                    <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TextBox.Style>
        </TextBox>

        <!-- Boutons d'action rapide -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <Button x:Name="ResetBtn" Content="↩ Réinitialiser"
                    Click="ResetBtn_Click"
                    Padding="10,6" FontSize="11" Cursor="Hand"
                    Background="#FFEBEE" Foreground="#E74C3C"
                    BorderThickness="1" BorderBrush="#FFCDD2"
                    ToolTip="Revenir au vocabulaire par défaut">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FFCDD2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>

        <!-- Boutons OK/Annuler -->
        <StackPanel Grid.Row="4" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Annuler" IsCancel="True"
                    Padding="14,7" Background="#ECF0F1" BorderThickness="1"
                    BorderBrush="#BDC3C7" Cursor="Hand" FontSize="12"
                    Margin="0,0,8,0"/>
            <Button x:Name="SaveBtn" Content="💾 Sauvegarder"
                    Click="SaveBtn_Click"
                    Padding="14,7" Background="#7B1FA2" Foreground="White"
                    BorderThickness="0" Cursor="Hand" FontSize="12">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#6A1B9A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
