<Window x:Class="MedCompanion.Dialogs.RateLetterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="⭐ Évaluer le courrier"
        Height="320"
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- Style pour les boutons étoiles -->
        <Style x:Key="StarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <TextBlock Text="{TemplateBinding Content}"
                                 FontSize="{TemplateBinding FontSize}"
                                 Foreground="{TemplateBinding Foreground}"
                                 Background="{TemplateBinding Background}"
                                 Cursor="Hand"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style pour le bouton Valider (simplifié) -->
        <Style x:Key="ValidateButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="#999999"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#45a049"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Grid.Row="0"
                   Text="Comment évaluez-vous ce courrier ?"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0,0,0,15"/>

        <!-- Zone étoiles -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20"
                Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Note :"
                          FontSize="13"
                          Foreground="#666"
                          Margin="0,0,0,10"/>
                
                <StackPanel Orientation="Horizontal" 
                           HorizontalAlignment="Center">
                    <Button x:Name="Star1Button" 
                            Content="☆"
                            Style="{StaticResource StarButtonStyle}"
                            Click="StarButton_Click"
                            Tag="1"/>
                    <Button x:Name="Star2Button" 
                            Content="☆"
                            Style="{StaticResource StarButtonStyle}"
                            Click="StarButton_Click"
                            Tag="2"/>
                    <Button x:Name="Star3Button" 
                            Content="☆"
                            Style="{StaticResource StarButtonStyle}"
                            Click="StarButton_Click"
                            Tag="3"/>
                    <Button x:Name="Star4Button" 
                            Content="☆"
                            Style="{StaticResource StarButtonStyle}"
                            Click="StarButton_Click"
                            Tag="4"/>
                    <Button x:Name="Star5Button" 
                            Content="☆"
                            Style="{StaticResource StarButtonStyle}"
                            Click="StarButton_Click"
                            Tag="5"/>
                </StackPanel>

                <TextBlock x:Name="RatingHintText"
                          Text="Cliquez sur une étoile pour noter"
                          FontSize="12"
                          Foreground="#999"
                          HorizontalAlignment="Center"
                          Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Commentaire optionnel -->
        <TextBlock Grid.Row="2"
                   Text="Commentaire (optionnel) :"
                   FontSize="13"
                   Foreground="#666"
                   Margin="0,0,0,5"/>

        <Border Grid.Row="3"
                Background="White"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="8"
                Margin="0,0,0,15">
            <TextBox x:Name="CommentTextBox"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     BorderThickness="0"
                     Padding="10"
                     FontSize="13"
                     Background="Transparent"/>
        </Border>

        <!-- Boutons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Annuler"
                    Width="100"
                    Height="35"
                    Margin="0,0,10,0"
                    Click="CancelButton_Click"
                    Style="{StaticResource {x:Type Button}}"/>
            
            <Button x:Name="ValidateButton"
                    Content="✓ Valider"
                    Width="100"
                    Height="35"
                    IsEnabled="False"
                    Click="ValidateButton_Click"
                    Style="{StaticResource ValidateButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
