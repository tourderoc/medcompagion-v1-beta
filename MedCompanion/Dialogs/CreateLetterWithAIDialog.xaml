<Window x:Class="MedCompanion.Dialogs.CreateLetterWithAIDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="✨ Créer un courrier avec l'IA intelligente"
        Width="600" Height="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        
        <Style TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock FontSize="18" FontWeight="Bold" Foreground="#2C3E50">
                ✨ Assistant Courrier Intelligent
            </TextBlock>
            <TextBlock FontSize="12" Foreground="#7F8C8D" Margin="0,5,0,0">
                Décrivez le courrier dont vous avez besoin et l'IA trouvera automatiquement le meilleur modèle pour vous.
            </TextBlock>
        </StackPanel>

        <!-- Étiquette -->
        <TextBlock Grid.Row="1" 
                   FontWeight="SemiBold" 
                   Foreground="#34495E"
                   Margin="0,0,0,8">
            📝 Décrivez votre demande :
        </TextBlock>

        <!-- Zone de saisie -->
        <Border Grid.Row="2" 
                BorderBrush="#BDC3C7" 
                BorderThickness="1" 
                CornerRadius="4"
                Background="White">
            <TextBox x:Name="RequestTextBox"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     BorderThickness="0"
                     Padding="10"
                     FontSize="13"
                     TextChanged="RequestTextBox_TextChanged">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Style.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <VisualBrush Stretch="None" AlignmentX="Left" AlignmentY="Top">
                                            <VisualBrush.Visual>
                                                <TextBlock Text="Exemple : &quot;Courrier pour demande de PAP à l'école pour un enfant avec trouble de l'attention&quot;"
                                                           Foreground="#95A5A6"
                                                           Margin="2,2,0,0"
                                                           FontSize="13"/>
                                            </VisualBrush.Visual>
                                        </VisualBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
            </TextBox>
        </Border>

        <!-- Compteur de caractères et infos -->
        <StackPanel Grid.Row="3" Margin="0,8,0,0">
            <DockPanel>
                <TextBlock x:Name="CharCountText" 
                           DockPanel.Dock="Right"
                           FontSize="11" 
                           Foreground="#95A5A6"
                           Text="0 caractères"/>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock FontSize="11" Foreground="#7F8C8D">
                        ℹ️ L'IA va analyser votre demande, chercher le MCC le plus pertinent et générer un courrier personnalisé avec les informations du patient.
                    </TextBlock>
                </StackPanel>
            </DockPanel>
            
            <!-- Barre de progression (cachée par défaut) -->
            <StackPanel x:Name="ProgressPanel" 
                        Visibility="Collapsed" 
                        Margin="0,10,0,0">
                <TextBlock x:Name="ProgressText" 
                           FontSize="12" 
                           Foreground="#2980B9"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,5"/>
                <ProgressBar x:Name="ProgressBar" 
                             Height="4" 
                             IsIndeterminate="True"/>
            </StackPanel>
        </StackPanel>

        <!-- Boutons d'action -->
        <Grid Grid.Row="4" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="CancelButton"
                    Grid.Column="0"
                    Content="Annuler"
                    HorizontalAlignment="Left"
                    Width="100"
                    Background="#ECF0F1"
                    Click="CancelButton_Click"/>

            <Button x:Name="GenerateButton"
                    Grid.Column="1"
                    Content="🚀 Analyser et Générer"
                    IsEnabled="False"
                    Width="180"
                    Background="#3498DB"
                    Foreground="White"
                    FontWeight="SemiBold"
                    Click="GenerateButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Padding" Value="12,6"/>
                        <Setter Property="Margin" Value="5"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#BDC3C7"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</Window>
