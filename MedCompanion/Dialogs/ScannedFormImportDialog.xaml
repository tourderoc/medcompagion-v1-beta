<UserControl x:Class="MedCompanion.Dialogs.ScannedFormImportDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Importer un formulaire scanné" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>

        <StackPanel Grid.Row="2" Orientation="Vertical">
            <Button x:Name="ImportPdfButton" Content="📁 Sélectionner un PDF" Height="40" FontSize="14" Background="#3498DB" Foreground="White" BorderThickness="0" Cursor="Hand" Click="ImportPdfButton_Click" Margin="0,0,0,10"/>
            <TextBlock x:Name="SelectedFileText" Text="Aucun fichier sélectionné" FontSize="12" Foreground="#7F8C8D" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" Content="💾 Enregistrer" Height="35" Width="120" FontSize="13" Background="#27AE60" Foreground="White" BorderThickness="0" Cursor="Hand" IsEnabled="False" Click="SaveButton_Click" Margin="0,0,10,0"/>
            <Button x:Name="CancelButton" Content="❌ Annuler" Height="35" Width="120" FontSize="13" Background="#E74C3C" Foreground="White" BorderThickness="0" Cursor="Hand" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
