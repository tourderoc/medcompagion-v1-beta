<Window x:Class="MedCompanion.Views.StateMachineEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:MedCompanion.Controls"
        mc:Ignorable="d"
        Title="Med Avatar State Machine Editor" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <!-- Menu/Toolbar -->
        <Border Grid.ColumnSpan="2" Background="#F0F0F0" Padding="10" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Content="💾 Save Profile" Click="OnSaveClick" Padding="10,5" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding CurrentProfile.Name}" VerticalAlignment="Center" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <!-- Editor Control -->
        <controls:StateMachineEditor x:Name="EditorControl" Grid.Row="1" Grid.Column="0"/>
        
        <!-- Inspector Panel - State or Transition -->
        <Grid Grid.Row="1" Grid.Column="1">
            <controls:StateInspector x:Name="StateInspectorControl" Visibility="Collapsed"/>
            <controls:TransitionInspector x:Name="TransitionInspectorControl" Visibility="Collapsed"/>

            <!-- Message quand rien n'est sélectionné -->
            <Border x:Name="NoSelectionPanel" Background="White" BorderBrush="#E0E0E0" BorderThickness="1,0,0,0">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Selectionnez un etat ou une transition" Foreground="#888" FontSize="14" TextAlignment="Center"/>
                    <TextBlock Text="pour voir ses proprietes" Foreground="#888" FontSize="12" TextAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
