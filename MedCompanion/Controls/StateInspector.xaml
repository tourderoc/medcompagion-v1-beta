<UserControl x:Class="MedCompanion.Controls.StateInspector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MedCompanion.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300"
             Background="White" BorderBrush="#E0E0E0" BorderThickness="1,0,0,0">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="State Properties" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Basic Info -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Name" Foreground="#757575" FontSize="12"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Padding="5"/>

            <TextBlock Text="Description" Foreground="#757575" FontSize="12"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Padding="5" Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

            <CheckBox IsChecked="{Binding IsLooping, Mode=TwoWay}" Content="Loop Animation" Margin="0,0,0,15"
                      Checked="OnLoopChanged" Unchecked="OnLoopChanged"/>
        </StackPanel>

        <!-- Media Playlist -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Media Playlist" Foreground="#757575" FontSize="12" Margin="0,0,0,5"/>
            <Button Content="+ Add Media" Click="OnAddMediaClick" HorizontalAlignment="Left" Padding="10,5" Margin="0,0,0,10"/>
        </StackPanel>

        <ListBox Grid.Row="3" ItemsSource="{Binding MediaSequence}" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding FilePath}" TextTrimming="CharacterEllipsis" ToolTip="{Binding FilePath}"/>
                        <Button Grid.Column="1" Content="❌" Click="OnRemoveMediaClick" Padding="5,2" Margin="5,0,0,0" Background="Transparent" BorderThickness="0" Foreground="Red"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
